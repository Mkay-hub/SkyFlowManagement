Project TODO / Issues

1) Incorrect namespace for DataHandling interface
   - File: Database/DataHandling.cs
   - Symptom: Other files use `using SkyFlowManagement.Database;` and expect `DataHandling` in that namespace. The file previously declared `namespace SkyFlowManagement.Model` which causes type lookup/compile errors.
   - Status: Fixed (namespace changed to SkyFlowManagement.Database).
   - Suggested follow-up: Run a full build to confirm no remaining type errors.

2) Thread usage without explicit using
   - File: UI/LoginScreen.cs
   - Symptom: LoginScreen calls `Thread.Sleep(1000);` but does not include `using System.Threading;`. The project may rely on global usings; if not, add the using.
   - Status: Fixed (added `using System.Threading;`)
   - Severity: Low
   - Suggested fix: Add `using System.Threading;` to the top of the file or enable global using for System.Threading.

3) Inconsistent method naming / style
   - Files: Render/TableCreate.cs, Model/*
   - Symptom: Methods use camelCase (e.g., `getColumnWidths`, `buildRow`) while public methods in other classes use PascalCase. This is a style inconsistency that may confuse maintainers.
   - Severity: Low
   - Suggested fix: Rename private methods to PascalCase (or keep as-is if project convention is different). No functional impact.

4) Typos and comments
   - Files: multiple (Database/SQLRepo.cs comments mention "Passanger", etc.)
   - Symptom: Misspellings in comments and some inline text.
   - Severity: Informational
   - Suggested fix: Clean up comments; does not affect compilation.

5) Validate database queries and null handling
   - Files: Database/SQLRepo.cs
   - Symptom: SQL queries use `SELECT *` and Map methods cast database fields directly. If DB schema changes or nulls appear, mapping can throw exceptions.
   - Status: Partially fixed (Passenger mapping now guards for DBNull on DateOfBirth and ContactNumber). Consider adding more defensive mappings and avoiding SELECT *.
   - Severity: Medium
   - Suggested fix: Add defensive checks, use column ordinals or named lookups carefully, and handle DBNull where appropriate.

6) Missing input validation / error handling
   - Files: Logic/FlightService.cs, UI/*
   - Symptom: Several user inputs are accepted and passed straight to the database (e.g., gate agent id). There is minimal verification that referenced IDs exist.
   - Status: Partially fixed (UpdateFlightStatus method completed to prompt and apply new status). Additional validation and try/catch blocks still recommended.
   - Severity: Medium
   - Suggested fix: Verify referenced IDs (e.g., GateAgent exists) before inserting; add try/catch around DB operations; show clear error messages.

7) Unit tests / CI
   - Symptom: No tests found in repository and no CI configuration.
   - Severity: Low/Medium
   - Suggested fix: Add unit tests for AuthService, SQLRepo (with test DB), and core logic classes. Add a CI workflow (GitHub Actions) to run build/tests.

Notes & Next steps:
- I updated the DataHandling.cs namespace (issue #1). Please run a full build and send the build output if you want me to continue fixing remaining compiler errors.
- If you want, I can also: (a) add missing using for Thread, (b) add defensive DB guards, (c) standardize naming conventions, or (d) create unit test scaffolding. Reply with which tasks to prioritize.

-- Generated by GitHub Copilot

SkyFlowManagement — Recent fixes

This README summarizes the small fixes applied (based on TODO.md) and how to commit them locally.

Changes applied:
- Database/DataHandling.cs: Corrected namespace to SkyFlowManagement.Database (fixes type lookup errors).
- UI/LoginScreen.cs: Added `using System.Threading;` to support Thread.Sleep usage.
- Database/SQLRepo.cs: Defensive DBNull checks added in MapPassenger for DateOfBirth and ContactNumber.
- Logic/FlightService.cs: Completed UpdateFlightStatus method and added `using System.Linq;`.
- TODO.md: Updated statuses for the above changes.

How to commit these changes locally (run in your repository root):

# Review changes
git status
git diff

# Stage files
git add Database/DataHandling.cs UI/LoginScreen.cs Database/SQLRepo.cs Logic/FlightService.cs TODO.md README.md

# Commit
git commit -m "Fix: DataHandling namespace; add Thread using; guard Passenger mapping; complete UpdateFlightStatus; update TODO"

# Push
git push origin HEAD

If you want me to create the commit directly I can't run git here; run the commands above in your terminal or tell me to generate a patch file you can apply with git.

Next recommended steps:
- Run a full build in Visual Studio and share build output for any remaining compiler errors.
- Add more defensive DB mapping and input validation.
- Consider adding unit tests and CI.

-- Generated by GitHub Copilot
